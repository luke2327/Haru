import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  barStyle: {
    paddingBottom: 0,
    backgroundColor: '#000',
    borderTopWidth: 1,
    borderTopColor: '#fff',
  },
});
