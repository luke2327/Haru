import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  header: {
    flex: 1,
    backgroundColor: '#000',
  },
  headerCenterComponent: {
    color: '#fff', 
    fontSize: 20, 
    fontWeight: 'bold',
  },
});
